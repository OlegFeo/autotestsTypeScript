export interface OrderContext {
  orderNumber: string;
  parcelNumber: string;
  orderId: string;
  parcelId: string;
  totalSum: string;
  sberLink: string;
  returnNumber: string;
  returnId: string;
  returnBoxNumber: string;
  returnBoxid: string;
  fiscalUuid: string;
}
